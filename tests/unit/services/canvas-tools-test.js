import { module, skip } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Service | canvas-tools', function(hooks) {
  setupTest(hooks);

  skip('select tool is selected by default');
  skip('will reset to previous tool when calling clearTool');
  skip('handle new sprite uses current tool to decide what to do');
});
